---
import type { CollectionEntry } from 'astro:content';
import IconWell from '../IconWell/IconWell.astro';

interface Props {
  project: CollectionEntry<'projects'>;
  alternate: boolean;
  level?: 'h2' | 'h3';
}

const { project, alternate, level: Heading = 'h2' } = Astro.props;

const {
  id,
  data: { title, teaser, accent },
} = project;
---

<article class:list={[{ alternate }]}>
  <IconWell
    elevation="low"
    project={project}
  />
  <div class="content">
    <Heading>
      <a
        data-astro-prefetch
        href={`/projects/${id}/`}
      >
        {title}
      </a>
    </Heading>
    <p>
      {teaser}
    </p>
  </div>
</article>

<style define:vars={{ 'accent-color': accent }}>
  article {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;

    @container content-list (inline-size >= 30rem) {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(0, 2fr);

      .icon-well {
        rotate: -1deg;
      }

      &.alternate {
        grid-template-columns: minmax(0, 2fr) minmax(0, 1fr);

        .icon-well {
          rotate: 1deg;
        }
      }
    }
  }

  .content {
    display: flex;
    flex-direction: column;
    gap: 1rem;

    @container content-list (min-inline-size: 30rem) {
      .alternate & {
        order: -1;
      }
    }
  }

  a {
    color: var(--accent-color);
  }
</style>
